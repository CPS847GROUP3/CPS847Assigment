# React App with server.js

Install packages through: 'npm i'
Run by typing 'npm run dev'
Can add questions to the text file, react component not really that necessary. All the code currently in server.js file.